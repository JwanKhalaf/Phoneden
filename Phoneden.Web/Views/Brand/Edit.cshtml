@using Phoneden.ViewModels
@model Phoneden.ViewModels.BrandViewModel

@{
    ViewBag.Title = "Update " + Model.Name;
}

<div class="page-title">
  <h3>Update Brand</h3>
</div>

@Html.EditorFor(m => m)

@section Scripts {
  @{   await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
