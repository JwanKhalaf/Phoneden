@using Phoneden.ViewModels
@model IEnumerable<Phoneden.ViewModels.CustomerViewModel>

@{
  ViewBag.Title = "Top 10 Customers";
}

<div class="page-title">
  <h3>Top 10 Customers</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <div class="grid simple">
      <div class="grid-body no-border">
        <table class="table table-striped table-flip-scroll cf">
          <thead class="cf">
            <tr>
              <th>
                Name
              </th>
              <th>
                Email
              </th>
              <th>
                Phone
              </th>
              <th>
                Allowed Credit
              </th>
            </tr>
          </thead>
          <tbody>
            @foreach (var customer in Model)
            {
              <tr>
                <td>
                  @customer.Name
                </td>
                <td>
                  @customer.Email
                </td>
                <td>
                  @customer.Phone
                </td>
                <td>
                  @customer.AllowedCredit
                </td>
                <td>
                  <a asp-controller="Customer"
                     asp-action="Details"
                     asp-route-id="@customer.Id"
                     class="btn btn-mini btn-success pull-right">
                    View
                  </a>
                </td>
              </tr>
            }
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
